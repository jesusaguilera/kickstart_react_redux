// Dependencies
import React from "react";

//Components
import Nav from "./Nav";

const Header = (props) => {
  return (
    <header>
      <div>logo</div>
      <Nav />
    </header>
  );
};

export default Header;
